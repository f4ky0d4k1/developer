DEVELOPER_ADDRESS=host.docker.internal:8081
ALLOY_REMOTE_WRITE_URL=
ALLOY_REMOTE_WRITE_USER=
ALLOY_REMOTE_WRITE_TOKEN=
# Логи docker-контейнеров (opencode) отправляются в Loki через те же секреты, что и
# логи приложения: LOKI_HOST / LOKI_USERNAME / LOKI_PASSWORD из корневого .env
# (передаются через env_file). Отдельные ALLOY_LOKI_* переменные не нужны.